name: CI

on:
  pull_request:
  push:
    branches:
      - main
      - dev

jobs:
  placeholder:
    name: Repository checks
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Placeholder
        run: |
          echo "Application code has not been scaffolded yet."
          echo "Add lint, typecheck, test, and build steps after the stack is initialized."

